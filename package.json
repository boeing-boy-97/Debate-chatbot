{
  "name": "debate-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Debate AI — Think. Argue. Improve. Debate an AI opponent that takes the opposite side.",
  "workspaces": [
    "frontend",
    "frontend/server"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --workspace debate-ai-server\" \"npm run dev --workspace debate-ai-frontend\"",
    "dev:backend": "npm run dev --workspace debate-ai-server",
    "dev:frontend": "npm run dev --workspace debate-ai-frontend",
    "build": "npm run build --workspace debate-ai-frontend",
    "start": "npm run start --workspace debate-ai-server",
    "test": "npm run test --workspace debate-ai-frontend"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "openai": "^4.77.0"
  },
  "overrides": {
    "qs": "^6.16.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
